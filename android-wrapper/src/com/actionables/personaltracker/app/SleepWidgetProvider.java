package com.actionables.personaltracker.app;

import android.appwidget.AppWidgetManager;
import android.content.Context;

public class SleepWidgetProvider extends QuickLogWidgetProvider {
    @Override protected void update(Context c, AppWidgetManager m, int id) {
        super.update(c,m,id);
    }
}
